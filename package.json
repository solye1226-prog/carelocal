{
  "name": "local-care-guide",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Pages static site for Korean regional hospital information guides.",
  "scripts": {
    "start": "python scripts/server.py",
    "check": "python scripts/check_links.py && python scripts/build-article-catalog.py --check && node --test scripts/test-analytics.mjs",
    "build:catalog": "python scripts/build-article-catalog.py",
    "refresh:hospitals": "node scripts/fetch-hira-hospitals.mjs"
  },
  "keywords": [
    "cloudflare-pages",
    "hospital",
    "seo",
    "static-site"
  ],
  "license": "UNLICENSED"
}
